# Зависимости конвейера. Список намеренно короткий: всё тяжёлое делают
# ffmpeg и модель расшифровки, остальное — арифметика по кадрам.

faster-whisper>=1.0        # расшифровка речи с пословным таймингом
numpy>=1.24                # огибающая звука, замеры, геометрия кадра
# 🔴 ВЕРХНЯЯ ГРАНИЦА ОБЯЗАТЕЛЬНА. В OpenCV 5.0 убран `cv2.CascadeClassifier`,
# на котором держится замер лица (`lico.py`). Без ограничения pip ставит 5.x, и
# конвейер падает на шаге `kadr.py` строкой «module 'cv2' has no attribute
# 'CascadeClassifier'» — то есть на пятом шаге из тринадцати, после самой долгой
# расшифровки.
opencv-python-headless>=4.8,<5    # поиск лица в кадре (замер крупностей)

# ffmpeg и ffprobe ставятся отдельно, пакетом системы — см. УСТАНОВКА.md
